span.pull-right(ng-cloak)
  ul.nav.navbar-nav
    li(ng-if="isAuthenticated()" ng-class="{ active: isActive('/admin') }")
      a(ng-href='/admin')
        i.fa.fa-unlock-alt.fa-fw.text-color-lock
        | Admin
  span(ng-show="isAuthenticated()")
    a.btn.btn-default.navbar-btn(href="auth/logout")
      | Logout
